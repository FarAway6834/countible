{
    "name": "C and Python Environment",
    "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
    "features": {
        "python": "3.x",
        "packages": ["python3-dev", "gcc"]
    },
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y python3-dev gcc"
}
